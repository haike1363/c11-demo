AUX_SOURCE_DIRECTORY(./src SRC_LIST)
ADD_LIBRARY(sub-so SHARED ${SRC_LIST})
SET_TARGET_PROPERTIES(sub-so PROPERTIES OUTPUT_NAME "sub")

# 设置动态库的版本号
SET_TARGET_PROPERTIES(sub-so PROPERTIES VERSION 0.0 SOVERSION 0)
SET(LIBRARY_OUTPUT_PATH .)